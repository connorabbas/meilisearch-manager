# v2

Goal: I want to make a v2 release of this meilisearch manager. The updates will be: New frontend UI component library using Nuxt UI and tailwind - retaining the same functionality with an updated look and feel based on the equivalent nuxt ui components but using their Dashboard components: https://ui.nuxt.com/docs/components#dashboard for a new sidebar layout. Implement the fairly new search rules condition filters: https://www.meilisearch.com/docs/reference/api/search-rules/create-or-update-a-search-rule#body-conditions-one-of-1-filter-one-of-1-values. Improve the search functionality (ability to use RAW search, facet filtering improvements, etc.).

The UI refactor is step one, so the new desired features dont need to be re-implemented. What is the best strategy for replacing the frontend? Wipe all the pages and CSS/TS config that has any primevue reference? Then page by page rebuild referencing the upstream branch as a reference? There are still several components that can survive, like the JSON editor, etc. We should still be able to use Lucide icons with Nuxt UI, so that's an easy transition as well. We can install and configure playwright MCP (opencode) to test the refactored frontend.

Give me a plan on how we can start the frontend refactor in a staged approach, what features/pages should be tackled in what order, and what the definition of done looks like to have PrimeVue completely removed, and Nuxt UI used as the new frontend component/theme system for this application. I have installed necessary MCP and skills for Nuxt UI, use them for planning and implementation of tis refactor to help accurately port the pages/components accurately also potentially improving them along the way.